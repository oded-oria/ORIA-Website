# Throwaway design prototypes and the before/after comparison page.
# They are working files only: they reference Google Fonts and must never be
# published to the live site (which has no third-party requests and no cookie banner).
prototype/
redesign-lab/
